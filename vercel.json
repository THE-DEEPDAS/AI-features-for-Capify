{
  "functions": {
    "api/getAdvice.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "buildCommand": "pip install -r requirements.txt --target pythonenv && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "PYTHONPATH": "pythonenv"
  }
}
